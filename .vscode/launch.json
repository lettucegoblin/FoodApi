{
    "version": "0.2.0",
    "configurations": [ 
        {
            "name": "Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://127.0.0.1:3000",      // create-react-app's default port 3000
            "webRoot": "${workspaceRoot}/food-api-react/src",
            "preLaunchTask": "npm: start"        // Add prelaunch Task npm: start (defined in tasks.json)
        }
    ]
}